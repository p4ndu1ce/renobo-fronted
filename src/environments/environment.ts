/** URLs base de las APIs (work, auth, config). */
export const environment = {
  production: false,
  /** API de trabajos, pagos, ingenieros, partners (work-service). */
  apiUrl: 'https://s6txacomrf.execute-api.us-east-1.amazonaws.com/dev',
  /** API de autenticación y perfil de usuario (auth-service). */
  authApiUrl: 'https://m587zdkcje.execute-api.us-east-1.amazonaws.com/dev',
  /** API de configuración pública (config-service). */
  configApiUrl: 'https://qvdde3mbs8.execute-api.us-east-1.amazonaws.com/dev',
};
